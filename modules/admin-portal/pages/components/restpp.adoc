= RESTPP
:server-name: RESTPP
:experimental:

You can configure RESTPP settings on the RESTPP component page:

image::restpp-page.png[]

The following parameters are configurable in Admin Portal

* Default query timeout

include::change-configuration.adoc[]
