= Application Server (GUI)
:server-name: Application

You can configure Application Server (GUI) settings through this page.
Application Server is the backend component powering both GraphStudio and Admin Portal.

image::gui-page.png[]

include::change-configuration.adoc[]
